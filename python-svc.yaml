apiVersion: v1
kind: Service
metadata: 
  name: python-app
spec:
  type: NodePort
  selector: 
    app: python
  ports:
  - name:
    protocol: TCP      
    port: 3000
    targetPort: 3000
    nodePort: 30011

